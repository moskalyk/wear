import { ProviderMessage } from '../../types';
import { WalletRequestHandler } from '../wallet-request-handler';
import { BaseWalletTransport } from '../base-wallet-transport';
/**
 * Initialized on Wallet side
 */
export declare class UnrealMessageHandler extends BaseWalletTransport {
    constructor(walletRequestHandler: WalletRequestHandler);
    register(windowHref?: string | URL): Promise<void>;
    unregister(): void;
    private onMessageFromUnreal;
    sendMessage(message: ProviderMessage<any>): void;
    private getUnrealTransportSession;
}
