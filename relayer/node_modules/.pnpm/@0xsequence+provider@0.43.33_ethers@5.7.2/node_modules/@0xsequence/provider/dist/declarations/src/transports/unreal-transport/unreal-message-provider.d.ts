import { OpenWalletIntent, ProviderMessage } from '../../types';
import { BaseProviderTransport } from '../base-provider-transport';
/**
 * Initialized on dApp side
 */
export declare class UnrealMessageProvider extends BaseProviderTransport {
    private walletURL;
    constructor(walletAppURL: string);
    register: () => void;
    unregister: () => void;
    openWallet: (path?: string, intent?: OpenWalletIntent, networkId?: string | number) => void;
    closeWallet(): void;
    private onUnrealCallback;
    sendMessage(message: ProviderMessage<unknown>): void;
}
